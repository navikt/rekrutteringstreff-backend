# Generated by `cplt init` — review before trusting.
# Detected: JVM (Gradle), Docker, TestContainers, Flyway

[propose]
# ⚠️  grants access to Docker socket — effectively root on the host
# Suggested by: apps/rekrutteringsbistand-aktivitetskort, apps/rekrutteringstreff-api, apps/rekrutteringstreff-minside-api
allow_docker = true  # uncomment after reviewing (from heuristic scan)
# Suggested by: (root), apps/rekrutteringsbistand-aktivitetskort, apps/rekrutteringstreff-api, apps/rekrutteringstreff-minside-api, buildSrc, technical-libs/testrapid
allow_jvm_attach = true
# Suggested by: apps/rekrutteringsbistand-aktivitetskort, apps/rekrutteringstreff-api, apps/rekrutteringstreff-minside-api
allow_localhost_any = true

[propose.allow]
ports = [5432]

# The following are machine-specific — add to ~/.config/cplt/config.toml:
# allow.read = ["~/.gradle/gradle.properties"]  # in personal config

